<UserControl x:Class="EasyMovie.Client.Views.WatchCalendarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="900">

    <UserControl.Resources>
        <!-- Today pulse animation -->
        <Storyboard x:Key="TodayPulseStoryboard" RepeatBehavior="Forever">
            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(Border.Opacity)" BeginTime="0:0:0">
                <EasingDoubleKeyFrame KeyTime="0:0:0" Value="1.0"/>
                <EasingDoubleKeyFrame KeyTime="0:0:1.2" Value="0.3"/>
                <EasingDoubleKeyFrame KeyTime="0:0:2.4" Value="1.0"/>
            </DoubleAnimationUsingKeyFrames>
        </Storyboard>
    </UserControl.Resources>

    <DockPanel>
        <!-- Header -->
        <StackPanel DockPanel.Dock="Top" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="{DynamicResource Calendar_Title}" FontSize="28" FontWeight="Bold"
                               VerticalAlignment="Center" />
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center"
                            HorizontalAlignment="Right">
                    <Button x:Name="PrevMonthBtn" Click="PrevMonthBtn_Click"
                            Style="{DynamicResource MaterialDesignIconButton}" Width="36" Height="36"
                            ToolTip="◀">
                        <Path Data="M15.41,16.58L10.83,12L15.41,7.41L14,6L8,12L14,18L15.41,16.58Z"
                              Fill="{DynamicResource MaterialDesignBody}" Width="16" Height="16" Stretch="Uniform" />
                    </Button>
                    <TextBlock x:Name="MonthYearText" Text="2026年6月" FontSize="20" FontWeight="SemiBold"
                               VerticalAlignment="Center" Margin="12,0" MinWidth="120" TextAlignment="Center" />
                    <Button x:Name="NextMonthBtn" Click="NextMonthBtn_Click"
                            Style="{DynamicResource MaterialDesignIconButton}" Width="36" Height="36"
                            ToolTip="▶">
                        <Path Data="M8.59,16.58L13.17,12L8.59,7.41L10,6L16,12L10,18L8.59,16.58Z"
                              Fill="{DynamicResource MaterialDesignBody}" Width="16" Height="16" Stretch="Uniform" />
                    </Button>
                </StackPanel>
            </Grid>

            <!-- Month Summary Bar -->
            <Border x:Name="SummaryBar" CornerRadius="8" Padding="16,10"
                    Background="{DynamicResource MaterialDesignCardBackground}"
                    Margin="0,12,0,0" Visibility="Collapsed">
                <StackPanel x:Name="SummaryPanel" Orientation="Horizontal" />
            </Border>
        </StackPanel>

        <!-- Calendar Grid -->
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Grid x:Name="CalendarGrid" Margin="0,0,0,24" />
        </ScrollViewer>
    </DockPanel>
</UserControl>
