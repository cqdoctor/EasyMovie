<UserControl x:Class="EasyMovie.Client.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="900"
             UseLayoutRounding="True">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="8,0,8,24">

            <TextBlock Text="{DynamicResource Dashboard_Title}" FontSize="28" FontWeight="Bold"
                       Margin="8,0,0,20" />

            <!-- 统计卡片行 -->
            <WrapPanel Margin="0,0,0,20">
                <Border CornerRadius="12" Width="155" Padding="16,14" Margin="0,0,10,10">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#7C4DFF" Offset="0"/>
                            <GradientStop Color="#B388FF" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Border.Effect>
                        <DropShadowEffect Color="#7C4DFF" Opacity="0.3" BlurRadius="8" ShadowDepth="2"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="{DynamicResource Dashboard_TotalMovies}" FontSize="11" Foreground="#FFFFFFCC" />
                        <TextBlock x:Name="TotalMoviesText" Text="0" FontSize="30" FontWeight="Bold" Foreground="White" />
                    </StackPanel>
                </Border>

                <Border CornerRadius="12" Width="155" Padding="16,14" Margin="0,0,10,10">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#4CAF50" Offset="0"/>
                            <GradientStop Color="#81C784" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Border.Effect>
                        <DropShadowEffect Color="#4CAF50" Opacity="0.3" BlurRadius="8" ShadowDepth="2"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="{DynamicResource Dashboard_MonthWatched}" FontSize="11" Foreground="#FFFFFFCC" />
                        <TextBlock x:Name="MonthWatchedText" Text="0" FontSize="30" FontWeight="Bold" Foreground="White" />
                    </StackPanel>
                </Border>

                <Border CornerRadius="12" Width="155" Padding="16,14" Margin="0,0,10,10">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#FF9800" Offset="0"/>
                            <GradientStop Color="#FFB74D" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Border.Effect>
                        <DropShadowEffect Color="#FF9800" Opacity="0.3" BlurRadius="8" ShadowDepth="2"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="{DynamicResource Dashboard_TotalHours}" FontSize="11" Foreground="#FFFFFFCC" />
                        <TextBlock x:Name="TotalHoursText" Text="0" FontSize="30" FontWeight="Bold" Foreground="White" />
                    </StackPanel>
                </Border>

                <Border CornerRadius="12" Width="155" Padding="16,14" Margin="0,0,10,10">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#E91E63" Offset="0"/>
                            <GradientStop Color="#F06292" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Border.Effect>
                        <DropShadowEffect Color="#E91E63" Opacity="0.3" BlurRadius="8" ShadowDepth="2"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="{DynamicResource Dashboard_AvgRating}" FontSize="11" Foreground="#FFFFFFCC" />
                        <TextBlock x:Name="AvgRatingText" Text="0" FontSize="30" FontWeight="Bold" Foreground="White" />
                    </StackPanel>
                </Border>

                <Border CornerRadius="12" Width="155" Padding="16,14" Margin="0,0,10,10">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#00BCD4" Offset="0"/>
                            <GradientStop Color="#4DD0E1" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Border.Effect>
                        <DropShadowEffect Color="#00BCD4" Opacity="0.3" BlurRadius="8" ShadowDepth="2"/>
                    </Border.Effect>
                    <StackPanel>
                        <TextBlock Text="{DynamicResource Dashboard_Favorites}" FontSize="11" Foreground="#FFFFFFCC" />
                        <TextBlock x:Name="FavoritesText" Text="0" FontSize="30" FontWeight="Bold" Foreground="White" />
                    </StackPanel>
                </Border>
            </WrapPanel>

            <!-- 最近添加 -->
            <TextBlock Text="{DynamicResource Dashboard_RecentlyAdded}" FontSize="18" FontWeight="SemiBold" Margin="8,0,0,12" />
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" Height="180">
                <ItemsControl x:Name="RecentAddedList">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Width="100" Height="150" Margin="0,0,12,0" CornerRadius="8"
                                    Background="{DynamicResource MaterialDesignCardBackground}"
                                    Cursor="Hand" MouseLeftButtonDown="MovieCard_Click">
                                <StackPanel>
                                    <Border Width="100" Height="133" CornerRadius="8,8,0,0"
                                            Background="{DynamicResource MaterialDesignDivider}">
                                        <Image Source="{Binding PosterData, Converter={StaticResource PosterImageConverter}}"
                                               Stretch="UniformToFill" />
                                    </Border>
                                    <TextBlock Text="{Binding Title}" FontSize="11" Margin="6,4"
                                               TextTrimming="CharacterEllipsis" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- 最近观看 -->
            <TextBlock Text="{DynamicResource Dashboard_RecentlyWatched}" FontSize="18" FontWeight="SemiBold" Margin="8,16,0,12" />
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" Height="180">
                <ItemsControl x:Name="RecentWatchedList">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Width="100" Height="150" Margin="0,0,12,0" CornerRadius="8"
                                    Background="{DynamicResource MaterialDesignCardBackground}"
                                    Cursor="Hand" MouseLeftButtonDown="MovieCard_Click">
                                <StackPanel>
                                    <Border Width="100" Height="133" CornerRadius="8,8,0,0"
                                            Background="{DynamicResource MaterialDesignDivider}">
                                        <Image Source="{Binding PosterData, Converter={StaticResource PosterImageConverter}}"
                                               Stretch="UniformToFill" />
                                    </Border>
                                    <TextBlock Text="{Binding Title}" FontSize="11" Margin="6,4"
                                               TextTrimming="CharacterEllipsis" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

        </StackPanel>
    </ScrollViewer>
</UserControl>