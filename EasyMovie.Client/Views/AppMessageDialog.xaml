<Window x:Class="EasyMovie.Client.Views.AppMessageDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Width="420" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}"
        ShowInTaskbar="False">

    <StackPanel Margin="24">
        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock x:Name="IconText" FontSize="24" Margin="0,0,12,0" VerticalAlignment="Top" />
            <TextBlock x:Name="MessageText" FontSize="14" Foreground="{DynamicResource MaterialDesignBody}"
                       TextWrapping="Wrap" MaxWidth="320" VerticalAlignment="Center" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button x:Name="OkButton" Content="OK"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Click="Ok_Click" Margin="0,0,8,0" />
            <Button x:Name="CancelButton" Content="{DynamicResource Msg_Cancel}"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Click="Cancel_Click" Visibility="Collapsed" />
        </StackPanel>
    </StackPanel>
</Window>
