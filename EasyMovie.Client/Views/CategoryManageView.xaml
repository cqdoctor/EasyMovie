<UserControl x:Class="EasyMovie.Client.Views.CategoryManageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- 左侧：分类树 -->
        <Border Grid.Column="0"
                Background="{DynamicResource MaterialDesignCardBackground}"
                CornerRadius="8" Padding="16" Margin="0,0,16,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,12">
                    <TextBlock Text="📁 分类树" FontSize="18" FontWeight="Bold"
                               VerticalAlignment="Center" />
                    <Button Style="{StaticResource MaterialDesignIconForegroundButton}"
                            HorizontalAlignment="Right"
                            Click="AddRootCategory_Click"
                            ToolTip="添加根分类">
                        <materialDesign:PackIcon Kind="FolderPlus" />
                    </Button>
                </Grid>

                <TreeView x:Name="CategoryTree"
                          Grid.Row="1"
                          SelectedItemChanged="CategoryTree_SelectedItemChanged"
                          Style="{StaticResource MaterialDesignTreeView}">
                    <TreeView.ItemTemplate>
                        <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="FolderOutline"
                                                         Width="16" Height="16"
                                                         VerticalAlignment="Center"
                                                         Margin="0,0,8,0" />
                                <TextBlock Text="{Binding Name}" VerticalAlignment="Center" />
                            </StackPanel>
                        </HierarchicalDataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>
            </Grid>
        </Border>

        <!-- 右侧：编辑区域 -->
        <Border Grid.Column="1"
                Background="{DynamicResource MaterialDesignCardBackground}"
                CornerRadius="8" Padding="24">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- 标题 -->
                <TextBlock x:Name="FormTitle" Grid.Row="0"
                           Text="选择一个分类，或添加新分类"
                           FontSize="20" FontWeight="Bold"
                           Margin="0,0,0,20" />

                <!-- 表单 -->
                <TextBox x:Name="CategoryNameBox"
                         Grid.Row="1"
                         materialDesign:HintAssist.Hint="分类名称 *"
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                         Margin="0,0,0,12" />

                <TextBox x:Name="CategoryDescBox"
                         Grid.Row="2"
                         materialDesign:HintAssist.Hint="描述（可选）"
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                         TextWrapping="Wrap" AcceptsReturn="True"
                         MinHeight="60" MaxHeight="100"
                         Margin="0,0,0,12" />

                <!-- 父分类信息 -->
                <TextBlock x:Name="ParentInfo" Grid.Row="3"
                           Foreground="{DynamicResource MaterialDesignHintForeground}"
                           FontSize="13" Margin="0,0,0,16" />

                <!-- 统计信息 -->
                <TextBlock x:Name="StatInfo" Grid.Row="4"
                           Foreground="{DynamicResource MaterialDesignHintForeground}"
                           FontSize="13" />

                <!-- 操作按钮 -->
                <StackPanel Grid.Row="5" Orientation="Horizontal"
                            HorizontalAlignment="Right" Margin="0,16,0,0">
                    <Button x:Name="DeleteBtn" Content="删除分类"
                            Style="{StaticResource MaterialDesignFlatButton}"
                            Foreground="{DynamicResource MaterialDesignErrorForeground}"
                            Click="DeleteBtn_Click"
                            Margin="0,0,8,0" />
                    <Button x:Name="AddChildBtn" Content="添加子分类"
                            Style="{StaticResource MaterialDesignFlatButton}"
                            Click="AddChildBtn_Click"
                            Margin="0,0,8,0" />
                    <Button Content="保存" Style="{StaticResource MaterialDesignRaisedButton}"
                            Click="SaveBtn_Click" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
