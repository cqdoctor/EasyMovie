<UserControl x:Class="EasyMovie.Client.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="700">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="0,0,0,24">
            <StackPanel>
                <TextBlock Text="⚙️ 系统设置" FontSize="28" FontWeight="Bold" Margin="0,0,0,24" />

                <Border Background="{DynamicResource MaterialDesignCardBackground}" CornerRadius="8" Padding="24" Margin="0,0,0,16">
                    <Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="🎨 主题" FontSize="18" FontWeight="Bold" Margin="0,0,0,16"/>
                        <StackPanel Grid.Row="1" Orientation="Horizontal">
                            <Button x:Name="SystemThemeBtn" Style="{StaticResource MaterialDesignRaisedButton}" Click="SystemTheme_Click" Width="120" Height="80" Margin="0,0,12,0">
                                <Button.ContentTemplate><DataTemplate><StackPanel><materialDesign:PackIcon Kind="Monitor" Width="24" Height="24"/><TextBlock Text="随系统" Margin="0,4,0,0" HorizontalAlignment="Center"/></StackPanel></DataTemplate></Button.ContentTemplate>
                            </Button>
                            <Button x:Name="DarkThemeBtn" Style="{StaticResource MaterialDesignRaisedButton}" Click="DarkTheme_Click" Width="120" Height="80" Margin="0,0,12,0">
                                <Button.ContentTemplate><DataTemplate><StackPanel><materialDesign:PackIcon Kind="WeatherNight" Width="24" Height="24"/><TextBlock Text="深色" Margin="0,4,0,0" HorizontalAlignment="Center"/></StackPanel></DataTemplate></Button.ContentTemplate>
                            </Button>
                            <Button x:Name="LightThemeBtn" Style="{StaticResource MaterialDesignRaisedButton}" Click="LightTheme_Click" Width="120" Height="80">
                                <Button.ContentTemplate><DataTemplate><StackPanel><materialDesign:PackIcon Kind="WhiteBalanceSunny" Width="24" Height="24"/><TextBlock Text="浅色" Margin="0,4,0,0" HorizontalAlignment="Center"/></StackPanel></DataTemplate></Button.ContentTemplate>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- TMDB Key -->
                <Border Background="{DynamicResource MaterialDesignCardBackground}" CornerRadius="8" Padding="24" Margin="0,0,0,16">
                    <Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="🌐 网络设置" FontSize="18" FontWeight="Bold" Margin="0,0,0,12"/>
                        <StackPanel Grid.Row="1">
                            <TextBox x:Name="TmdbKeyBox" materialDesign:HintAssist.Hint="TMDB API Key" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Width="350" Margin="0,0,0,8"/>
                            <TextBox x:Name="ProxyBox" materialDesign:HintAssist.Hint="HTTP 代理 (如 http://127.0.0.1:7890)" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Width="350" Margin="0,0,0,8"/>
                            <TextBox x:Name="DoubanCookieBox" materialDesign:HintAssist.Hint="豆瓣 Cookie (登录后从F12复制)" Style="{StaticResource MaterialDesignFloatingHintTextBox}" Width="400" Margin="0,0,0,8" TextWrapping="Wrap" MinHeight="50"/>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="保存" Style="{StaticResource MaterialDesignFlatButton}" Click="SaveNetwork_Click" Margin="0,0,8,0"/>
                                <TextBlock Text="TMDB Key: themoviedb.org/settings/api" FontSize="11" Foreground="{DynamicResource MaterialDesignHintForeground}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- 导入导出 -->
                <Border Background="{DynamicResource MaterialDesignCardBackground}" CornerRadius="8" Padding="24" Margin="0,0,0,16">
                    <Grid>
                        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="📦 导入导出" FontSize="18" FontWeight="Bold" Margin="0,0,0,12"/>
                        <StackPanel Grid.Row="1" Margin="0,0,0,12" Orientation="Horizontal">
                            <TextBox x:Name="FolderPathBox" Width="300" materialDesign:HintAssist.Hint="文件夹路径"
                                     Style="{StaticResource MaterialDesignFloatingHintTextBox}" Margin="0,0,8,0" />
                            <Button Content="选择文件夹导入" Style="{StaticResource MaterialDesignRaisedButton}" Click="ImportFolder_Click" Margin="0,0,8,0"/>
                            <CheckBox x:Name="RecursiveCheck" Content="包含子文件夹" VerticalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel Grid.Row="2" Margin="0,0,0,12" Orientation="Horizontal">
                            <TextBlock Text="导出:" FontSize="13" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <Button Content="CSV" Style="{StaticResource MaterialDesignRaisedButton}" Click="ExportCsv_Click" Margin="0,0,6,0"/>
                            <Button Content="JSON" Style="{StaticResource MaterialDesignRaisedButton}" Click="ExportJson_Click" Margin="0,0,6,0"/>
                            <Button Content="全量备份" Style="{StaticResource MaterialDesignRaisedButton}" Click="ExportFullBackup_Click"/>
                        </StackPanel>
                        <StackPanel Grid.Row="3" Margin="0,0,0,12" Orientation="Horizontal">
                            <TextBlock Text="导入:" FontSize="13" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <Button Content="CSV" Style="{StaticResource MaterialDesignRaisedButton}" Click="ImportCsv_Click" Margin="0,0,6,0"/>
                            <Button Content="JSON" Style="{StaticResource MaterialDesignRaisedButton}" Click="ImportJson_Click" Margin="0,0,6,0"/>
                            <Button Content="数据还原" Style="{StaticResource MaterialDesignRaisedButton}" Click="RestoreBackup_Click"/>
                        </StackPanel>
                        <StackPanel Grid.Row="4" Orientation="Horizontal">
                            <TextBlock Text="数据库:" FontSize="13" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <Button Content="备份数据库" Style="{StaticResource MaterialDesignRaisedButton}" Click="BackupDbFile_Click" Margin="0,0,6,0"/>
                            <Button Content="还原数据库" Style="{StaticResource MaterialDesignRaisedButton}" Click="RestoreDbFile_Click"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Background="{DynamicResource MaterialDesignCardBackground}" CornerRadius="8" Padding="24" Margin="0,0,0,16">
                    <Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="💾 数据" FontSize="18" FontWeight="Bold" Margin="0,0,0,16"/>
                        <StackPanel Grid.Row="1"><TextBlock Text="数据库: %LocalAppData%/EasyMovie/" FontSize="13"/></StackPanel>
                    </Grid>
                </Border>

                <Border Background="{DynamicResource MaterialDesignCardBackground}" CornerRadius="8" Padding="24">
                    <Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="ℹ️ 关于" FontSize="18" FontWeight="Bold" Margin="0,0,0,16"/>
                        <StackPanel Grid.Row="1">
                            <TextBlock Text="EasyMovie v1.0" FontSize="14" FontWeight="Bold"/>
                            <TextBlock Text="WPF + .NET 10 + SQLite" FontSize="13" Foreground="{DynamicResource MaterialDesignHintForeground}"/>
                            <TextBlock Text="数据源: 豆瓣 / TMDB / 1905 / 猫眼" FontSize="13" Foreground="{DynamicResource MaterialDesignHintForeground}"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>
