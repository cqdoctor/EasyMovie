<Window x:Class="EasyMovie.Client.Views.CollectionEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="{DynamicResource Collection_EditTitle}"
        Width="400" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}">

    <StackPanel Margin="24">
        <TextBox x:Name="NameBox" materialDesign:HintAssist.Hint="{DynamicResource Collection_Name}"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,0,0,16" />
        <TextBox x:Name="DescBox" materialDesign:HintAssist.Hint="{DynamicResource Collection_Description}"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,0,0,16"
                 AcceptsReturn="True" Height="80" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" />
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{DynamicResource Msg_Cancel}" Style="{StaticResource MaterialDesignOutlinedButton}"
                    Click="Cancel_Click" Margin="0,0,8,0" />
            <Button Content="{DynamicResource CatTag_Save}" Style="{StaticResource MaterialDesignRaisedButton}"
                    Click="Save_Click" />
        </StackPanel>
    </StackPanel>
</Window>
