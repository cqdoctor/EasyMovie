<UserControl x:Class="MovieManager.Client.Views.OnlineSearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="🌐 在线搜索添加电影" FontSize="22"
                   FontWeight="Bold" Margin="0,0,0,16" />

        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="SearchBox" Grid.Column="0"
                     materialDesign:HintAssist.Hint="输入片名搜索..."
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     KeyDown="SearchBox_KeyDown" Margin="0,0,12,0" />
            <Button Grid.Column="1" Content="搜索" Style="{StaticResource MaterialDesignRaisedButton}"
                    Click="Search_Click" Margin="0,0,8,0">
                <Button.ContentTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Magnify" Margin="0,0,6,0" />
                            <TextBlock Text="搜索" />
                        </StackPanel>
                    </DataTemplate>
                </Button.ContentTemplate>
            </Button>
            <TextBlock x:Name="SourceLabel" Grid.Column="2" FontSize="12"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource MaterialDesignHintForeground}" />
        </Grid>

        <Border Grid.Row="2" Background="{DynamicResource MaterialDesignCardBackground}"
                CornerRadius="8" Padding="8">
            <Grid>
                <ListBox x:Name="ResultListBox"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         Style="{StaticResource MaterialDesignListBox}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="12" Margin="0,2">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="70" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0" Width="56" Height="80"
                                            CornerRadius="4" ClipToBounds="True"
                                            Background="{DynamicResource MaterialDesignDarkBackground}">
                                        <Image Source="{Binding PosterUrl}" Stretch="UniformToFill"
                                               RenderOptions.BitmapScalingMode="Fant" />
                                    </Border>
                                    <StackPanel Grid.Column="1" Margin="12,0,0,0" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Title}" FontSize="15"
                                                   FontWeight="Bold" TextTrimming="CharacterEllipsis" />
                                        <TextBlock FontSize="13" Margin="0,2,0,0"
                                                   Foreground="{DynamicResource MaterialDesignHintForeground}">
                                            <Run Text="{Binding Year, StringFormat='{}{0}'}" />
                                            <Run Text=" · " />
                                            <Run Text="{Binding Director}" />
                                        </TextBlock>
                                        <TextBlock Text="{Binding Synopsis}" FontSize="12"
                                                   TextTrimming="CharacterEllipsis"
                                                   Foreground="{DynamicResource MaterialDesignHintForeground}"
                                                   MaxHeight="32" Margin="0,3,0,0" />
                                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                            <TextBlock FontSize="12"
                                                       Foreground="{DynamicResource MaterialDesignSecondaryForeground}">
                                                <Run Text="⭐" />
                                                <Run Text="{Binding Rating, StringFormat='{}{0:F1}'}" />
                                                <Run Text=" · " />
                                                <Run Text="{Binding Source, StringFormat='来源: {0}'}" />
                                            </TextBlock>
                                        </StackPanel>
                                    </StackPanel>
                                    <Button Grid.Column="2" Style="{StaticResource MaterialDesignIconForegroundButton}"
                                            Click="AddResult_Click" Tag="{Binding}" ToolTip="添加到电影库"
                                            VerticalAlignment="Center">
                                        <materialDesign:PackIcon Kind="PlusCircleOutline" Width="28" Height="28" />
                                    </Button>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <StackPanel x:Name="EmptyPanel" VerticalAlignment="Center"
                            HorizontalAlignment="Center" Visibility="Collapsed">
                    <materialDesign:PackIcon Kind="MovieSearchOutline" Width="48" Height="48"
                                             Foreground="{DynamicResource MaterialDesignHintForeground}" />
                    <TextBlock x:Name="EmptyText" Text="搜索豆瓣或 TMDB 添加电影 🎬"
                               FontSize="14" HorizontalAlignment="Center"
                               Foreground="{DynamicResource MaterialDesignHintForeground}" Margin="0,8,0,0" />
                </StackPanel>

                <StackPanel x:Name="LoadingPanel" VerticalAlignment="Center"
                            HorizontalAlignment="Center" Visibility="Collapsed">
                    <materialDesign:PackIcon Kind="Sync" Width="32" Height="32"
                                             Foreground="{DynamicResource MaterialDesignSecondaryForeground}">
                        <materialDesign:PackIcon.RenderTransform>
                            <RotateTransform />
                        </materialDesign:PackIcon.RenderTransform>
                    </materialDesign:PackIcon>
                    <TextBlock Text="搜索中..." FontSize="14" HorizontalAlignment="Center" Margin="0,8,0,0" />
                </StackPanel>
            </Grid>
        </Border>

        <StackPanel Grid.Row="3" HorizontalAlignment="Right" Orientation="Horizontal" Margin="0,12,0,0">
            <TextBlock Text="豆瓣/TMDB" FontSize="12"
                       Foreground="{DynamicResource MaterialDesignHintForeground}"
                       VerticalAlignment="Center" Margin="0,0,12,0" />
            <Button Content="关闭" Style="{StaticResource MaterialDesignFlatButton}" Click="Close_Click" />
        </StackPanel>
    </Grid>
</UserControl>
